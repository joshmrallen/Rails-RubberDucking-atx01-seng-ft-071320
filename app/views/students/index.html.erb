<h1>These are some cool students:</h1>

<ul>
  <% @students.each do |student| %>
    <li> <%= link_to student.name, student_path(student.id) %> </li>
  <% end %>
</ul>

<br>
<br>
<p><%= link_to 'Add New Student', new_student_path %> | <%= link_to 'Ducks Main', ducks_path %></p>

